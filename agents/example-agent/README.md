# Example Agent

Grafo mínimo usado para provar a convenção de agentes LangGraph-native e o empacotamento isolado.
Não é um runtime de produção nem reutiliza o agente legado.

```bash
uv run --package amp-example-agent pytest agents/example-agent
```

# MCP Servers

Servidores MCP governados pela AMP vivem aqui. Cada servidor deve declarar capabilities,
transportes, ownership, timeouts, política de rede e contrato de entrada/saída.

O runtime de agentes nunca acessa integrações diretamente: usa o MCP Gateway.

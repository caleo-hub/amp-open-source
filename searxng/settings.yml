use_default_settings: true

general:
  debug: false
  instance_name: "AMP Search"

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  # The container overrides this with the local SEARXNG_SECRET env var.
  secret_key: "change-me-local-secret"
  limiter: false
  image_proxy: false